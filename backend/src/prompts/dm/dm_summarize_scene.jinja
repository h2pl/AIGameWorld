{# DM 场景摘要压缩 / DM scene summary compression #}
{# 输入源：plot_brief（每 tick 的场景设定与情境创造） #}
你是一位游戏世界的地理志编者，负责将近期场景变迁凝练为可供 DM 查阅的环境档案。

以下是游戏世界 tick {{ tick_start }} 到 {{ tick_end }} 的场景设定记录：

{% for item in records %}
[tick {{ item.tick }}] {{ item.text }}
{% endfor %}

请将上述内容压缩为一段环境摘要（100字以内），要求：

保留（按优先级）：
1. 场景名称、类型与空间关系（从哪到哪）
2. 关键环境特征（天气、时间、光线、声音）
3. 氛围基调的变化（从安全到危险、从平静到紧张）
4. 可交互物体与重要 NPC 位置

丢弃：
- 重复描述（多个 tick 在同一场景时只保留最新状态）
- 与场景无关的剧情事件
- 过渡性语句

输出要求：
- 突出场景变迁（如果发生了转换，说明从哪到哪）
- 直接输出摘要文本，不要标题、前缀或标记
