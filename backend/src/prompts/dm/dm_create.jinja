{# Phase 1: DM 创造情境 / DM creates the scene #}

{# === 场景列表 / Available scenes === #}
{% if scenes %}
当前世界的场景列表（请从以下 id 中选择）：
{% for s in scenes %}
- {{ s.id }}：{{ s.name }}（{{ s.description }}）
{% endfor %}
{% endif %}

{# === 上下文注入 / Context injection #}
{% if recent_summary %}
最近摘要：{{ recent_summary }}
{% endif %}

{% if plot_brief_prev %}
上一步剧情：{{ plot_brief_prev }}
{% endif %}

{# === 输出约束 / Output spec === #}
严格按以下 JSON 结构输出，不要输出任何其他内容（不要 markdown 代码块标记）。

字段约束：
- hints（string[]）：2-4 条环境提示。DM 不直接指引玩家行动，只描述环境中可感知的信息。
  必须使用简体中文。好的示例："地上有血迹通向左边走廊"、"右边房间传来低语"、"北门被重锁封死"。坏的示例："检查祭坛"、"攻击怪物"（直接命令，不是环境提示）。
- plot_brief（string）：2-3 句场景设置。描述角色行动"之前"发生了什么，不要写角色已做的事。
  必须使用简体中文。
- scene_id（string）：当前场景 id。必须从上方场景列表中精确选择一个 id，不得编造。

输出格式：
{
  "hints": ["...", "..."],
  "plot_brief": "...",
  "scene_id": "tavern"
}

示例：
{
  "hints": [
    "祭坛上的刻文在月光下泛着冷光",
    "旅人腰间的刀柄上刻着与祭坛相同的符文",
    "石塔方向传来一声沉闷的钟响，惊起一群乌鸦"
  ],
  "plot_brief": "雾气在十字路口聚拢，一座布满苔藓的祭坛立在路中央。旅人倚着祭坛擦拭长刀，斗篷下露出半截符文卷轴。",
  "scene_id": "crossroad"
}
