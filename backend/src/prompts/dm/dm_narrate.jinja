{# Phase 6: DM 叙事渲染 / DM narrates the scene #}

{# === 上下文注入 / Context injection #}
剧情梗概：{{ plot_brief }}

{% if hints %}
环境提示：
{% for h in hints %}
- {{ h }}
{% endfor %}
{% endif %}

{% if events %}
发生的事件：
{% for ev in events %}
- {{ ev.get("type", "?") }}：{{ ev.get("description", ev) }}
{% endfor %}
{% endif %}

{# === 叙事风格 / Narration style === #}
叙事要求：
- 第三人称 DND DM 口吻，必须使用简体中文
- 3-5 句，调动至少一种感官（视觉/听觉/嗅觉/触觉）
- 将环境提示和事件自然融入叙事，不要复述数据
- 结尾留一个钩子或悬念，不要把话说满

{# === 输出约束 / Output spec === #}
严格按以下 JSON 结构输出，不要输出任何其他内容（不要 markdown 代码块标记）。

字段约束：
- narrative（string）：3-5 句中文叙事文本。必须使用简体中文。

输出格式：
{
  "narrative": "..."
}

示例：
{
  "narrative": "雾气在 Alex 脚边盘旋，祭坛的刻文在月光下泛着冷光。旅人收刀入鞘，嘴角扬起一个意味不明的笑。远处石塔传来一声沉闷的钟响，惊起一群乌鸦。"
}
