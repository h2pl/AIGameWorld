{# 交互 prompt — LLM 裁决交互结果 + 旁白 / Interact: LLM judges result + narration #}
{# 剧情注入 / Plot injection #}
当前剧情情境：{{ plot_brief }}

{# DM 提示 / DM hints #}
{% if hints %}
DM 提示：
{% for hint in hints %}
- {{ hint }}
{% endfor %}
{% endif %}

{# PC 记忆 / PC memories #}
{% if memories %}
你记得：
{% for mem in memories %}
- {{ mem }}
{% endfor %}
{% endif %}

当前场景：{{ scene.name | default("未知场景") }}（{{ scene.type | default("") }}）
{{ scene.description or "无" }}
{% if scene.tilemap_summary %}
地图语义摘要：{{ scene.tilemap_summary }}
{% endif %}

交互目标详情：
- 名称：{{ obj.name }}
- 类型：{{ obj.object_type }}
- 位置：({{ obj.position_x | default(0) }}, {{ obj.position_y | default(0) }})
{% if obj.description %}- 外观：{{ obj.description }}{% endif %}
{% if obj.interact_data %}- 特性：{{ obj.interact_data | tojson }}{% endif %}

{{ pc.name }} 走到 {{ obj.name }} 旁，准备与之交互。

生成要求：
1. 根据 PC 身份性格、物体类型和特性、当前场景氛围，合理判断交互是成功还是失败。
2. 成功时描述 PC 如何巧妙或果断地达成目的；失败时描述遇到了什么阻碍或意外。
3. 结果要有戏剧性和故事感，避免平淡无奇的"打开了一个箱子"。

严格按以下 JSON 结构输出，不要输出任何其他内容（不要 markdown 代码块标记）：
{
  "success": <true 或 false>,
  "narration": "<1-2句中文第三人称旁白>"
}

示例 1（成功打开宝箱）：
{
  "success": true,
  "narration": "盗贼用细铁丝轻巧地挑开了铁锁，箱盖缓缓掀开。里面躺着一柄泛着蓝光的匕首和几枚金币，匕首的握柄上刻着古老的精灵符文。"
}
示例 2（试图操控祭坛但失败）：
{
  "success": false,
  "narration": "法师将手掌按在祭坛的符文上低声吟诵，但刻文突然爆发出一道刺目的红光，将他弹开了两步。空气中弥漫着烧焦的硫磺味，祭坛似乎排斥着凡人的触碰。"
}
示例 3（成功翻找书桌发现密信）：
{
  "success": true,
  "narration": "战士拉开书桌的抽屉，在一叠泛黄的账本下发现了一封火漆封口的密信。信封上没有署名，但封蜡上的图案和酒馆招牌上的纹章一模一样。"
}
