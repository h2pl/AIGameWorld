{# Phase 3: Actor 浅层决策 / Actor shallow decision #}
{% include '_character_system.jinja' %}

{# === 输出约束 / Output spec === #}
严格按以下 JSON 结构输出，不要输出任何其他内容（不要 markdown 代码块标记）：

字段说明：
- action_type: move / talk / attack / interact / wait 之一
- target: 目标角色 id 或场景对象 id；无则 null
- reasoning: 你的决策理由（2-3 句中文，体现你的角色功能和 DM 注入的动机）

输出格式：
{
  "action_type": "talk",
  "target": "pc_id",
  "reasoning": "作为一个商人，看到新面孔应该主动推销货物。DM 让我留意可疑的外来者。"
}

示例：
{
  "action_type": "talk",
  "target": "alex",
  "reasoning": "这个冒险者装备精良，生意来了。DM 提醒我最近要留意打听商队消息的人。"
}
