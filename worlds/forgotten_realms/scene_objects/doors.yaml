# 门定义 / Door Definitions

objects:
id:                          # 唯一标识 / Unique ID
name:                        # 名称 / Name
object_type:                 # 对象类型 / Object Type
interactable:                # 是否可交互 / Interactable
    interact_data:
    locked:                    # 是否锁定 / Locked
      key_id: "rusty_key"
    leads_to:                  # 通向 / Leads To
